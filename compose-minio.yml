services:
  minio:
    image: quay.io/minio/minio:latest
    container_name: minio-server
    ports:
      - ${MINIO_API_PORT}:9000 # S3 API Port
      - ${MINIO_CONSOLE_PORT}:9001 # Web Console Port
    environment:
      MINIO_ROOT_USER: ${MINIO_ROOT_USER}
      MINIO_ROOT_PASSWORD: ${MINIO_ROOT_PASSWORD}
    volumes:
      - minio_data:/data
    command: server --console-address ":${MINIO_CONSOLE_PORT}" /data
    healthcheck:
      test: ["CMD", "mc", "ready", "local"]
      interval: 5s
      timeout: 5s
      retries: 5
    networks:
      - preflight_pf-net

  # minio-init-buckets:
  #   image: quay.io/minio/mc:latest
  #   depends_on:
  #     minio:
  #       condition: service_healthy
  #   entrypoint: >
  #     /bin/sh -c "
  #     until (/usr/bin/mc alias set myminio http://minio:9000 ${MINIO_ROOT_USER} ${MINIO_ROOT_PASSWORD}) do echo 'Waiting for MinIO server...' && sleep 1; done;
  #     /usr/bin/mc mb myminio/${MINIO_BUCKET};
  #     /usr/bin/mc admin accesskey create myminio minioadmin
  #     exit 0;
  #     "

volumes:
  minio_data:
networks:
  preflight_pf-net: # Check by using `docker network ls` command
    external: true
